const admin = require("./admin");
const checklist = require("./checklist");
const user = require("./user");
const mainactivity = require("./mainactivity");

module.exports = { admin, checklist, user, mainactivity };
